<html>
    <?php
        echo("Hello world");
        $bloop = cat
        /* 
        "$***" a container useful for repeating info 

        Main data type:
        strings = "xxx"
        integer = whole number no decimal
        float = decimals are allowed
        boolean = isXXX true or false data type etc
        null = no value

        Functions:
        strtolower(insertstring) make all lowercase

        strtoupper(inserstring) make all uppercase

        strlen() get how many chars are in the string

        $string [0] [] function lets u get the string on specific position, in this case the first letter of the string
        
        str_replace(original, replace, $bloop) replace specifics

        substr($bloop, xx start index, length)
        */
        
    ?>
</html>